const DefaultState = {
  loading: false,
  data: []
}

const PokemonListReducer = (state = DefaultState, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default PokemonListReducer;